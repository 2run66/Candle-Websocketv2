version: '3'

services:
  my-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"  # Adjust port mapping as needed
    volumes:
      - ./candle-websocket-api.py:/app/candle-websocket-api.py
      - ./commons.py:/app/commons.py